---
title: "Refatorando os códigos"
author: "Doriedson"
date: "2025-05-01"
output:
  pdf_document: default
  html_document: default
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

require(ggplot2)

```

### Versão de 'Weibull_Carta.R'

```{r função da tabela 1}
pracma::clear()

weibull_carta_xbar = function(gama0=2, delta0=2,
                              gama1=c(.1, .5, 1, 1.5, 2, 2.5, 3, 4, 5, 6),
                              n=5, corridas=50*10**6, vetordelta){
  # Essa função faz a Tabela 1: Valores de ARL's obtidos para análise de desempenho
  # do gráfico de X_barra (mudança bidirecional)
  
  # gama0 e delta0 são os Parâmetros sob controle.
  # gama1 e vetordelta (='delta1') são os parâmetros fora de controle
  # n é o tamanho amostral
  # corridas é a quantidade de repetição em cada iteração
  
  # Média da distribuição
  mu0 <- gama0*(gamma(1/delta0+1))
  
  # Desvio padrão
  Sigma20 <- (gama0^2)*(gamma(2/delta0+1)-(gamma(1/delta0+1)^2))
  Sigma0 <- Sigma20^0.5
  
  
  # Distribuição Sob H0 (sob controle)
  
  #corridas<-50000000
  #Corridas Amostras de tamanho n

  SC0 <- (matrix(rweibull(n*corridas, delta0, gama0), n, corridas))
  SCmean0 <- colMeans(SC0) #Média das corridas amostras
  
  # Limites de Controle
  L<-quantile(SCmean0, c(pnorm(-3), 1-pnorm(-3)))
  LSC=L[[2]]
  LIC=L[[1]]
  
  ARL0<-1/(mean(SCmean0 <= LIC) + (mean(SCmean0 >= LSC)))

  tabela = data.frame(matrix(#c(sigma0, delta0, ARL0, mu0, sigma0, LIC, LSC),
                             0, ncol=7)
                      )
  colnames(tabela) <- c("gama1", "delta1", "ARL1",
                         "mu1", "sigma1", "desvio", "d(%)")
  for(delta1 in vetordelta){
    for(valor in gama1){
      # Distribuição sob H1 (Fora de Controle)
      mu1 <- valor*(gamma(1/delta1+1))
      Sigma21 <- (valor^2)*(gamma(2/delta1+1)-(gamma(1/delta1+1)^2))
      Sigma1 <- Sigma21^0.5
      
      # Distribuição Sob H1 (sob controle)
      SC1<-(matrix(rweibull(n*corridas, delta1, valor),n,corridas)) #Corridas Amostras de tamanho n
      SCmean1<-colMeans(SC1) #Mean das corridas amostras
      
      ARL1 <- 1/(mean(SCmean1<=LIC) + (mean(SCmean1>=LSC)))
      
      # Cálculo do desvio (diferença absoluta das médias) e de d (diferença relativa)
      desvio = mu0 - mu1
      d = ifelse(desvio/mu0 < 1, round(desvio/mu0, 2)*-100, round(desvio/mu0, 2)*100)
      tabela = rbind(tabela, c(valor, delta1,
                               round(ARL1, 4), round(mu1, 4),
                               round(Sigma1, 4), round(desvio, 4), d))
      # As casas decimais serão formatadas na planilha
    }
    cat('\nFeito: delta1=', delta1)
    }
    
    cat("\nSob controle\n\n")
    print(data.frame(valor=c('gama_0' = round(gama0, 4), 'delta_0'=round(delta0, 4),
                             'ARL_0' = round(ARL0, 4), 'mu_0' = round(mu0, 4),
                             'sigma_0'= round(Sigma0, 4), 'LIC' = round(LIC, 4),
                             'LSC' = round(LSC, 4))))
  tabela <- tabela[-1, ]
  # colnames(tabela) <- c("gama1", "delta1", "ARL1",
  #                       "mu1", "sigma1", "desvio", "d(%)")
  return(tabela)
}

```

```{r eval=FALSE}
# Para o número de corridas padrão cada iteração demora, em média, 5 min.
set.seed(2025)
for(x in c(.5, 1, 1.5, 2, 3, 5, 10, 20)){
  pracma::tic()
  
  tab = weibull_carta_xbar(vetordelta = x)
  #write.csv(tab, file="tabela1.csv", append = TRUE)
  write.table(tab, "tabela1.csv", sep = ",", col.names = !file.exists("tabela1.csv"),
              row.names = F, append = T)
  
  pracma::toc()
}
 
```

```{r tabela a1, eval=FALSE}
# Para o número de corridas padrão cada iteração demora, em média, 5 min.
set.seed(2025)
for(x in c(.5, 1, 1.5, 2, 3, 5, 10, 20)){
  pracma::tic()
  
  tab = weibull_carta_xbar(gama0 = 2, delta0 = 3, vetordelta = x)
  #write.csv(tab, file="tabela1.csv", append = TRUE)
  write.table(tab, "tabela_a1.csv", sep = ",", col.names = !file.exists("tabela_a1.csv"),
              row.names = F, append = T)
  
  pracma::toc()
}

# gama_0	2.0000			
# delta_0	3.0000			
# ARL_0	370.3978			
# mu_0	1.7860			
# sigma_0	0.6491			
# LIC	0.9600			
# LSC	2.6735

```

```{r tabela a2, eval=FALSE}
# Para o número de corridas padrão cada iteração demora, em média, 5 min.
set.seed(2025)
for(x in c(.5, 1, 1.5, 2, 3, 5, 10, 20)){
  pracma::tic()
  
  tab = weibull_carta_xbar(gama0 = .5, delta0 = 1, vetordelta = x)
  #write.csv(tab, file="tabela1.csv", append = TRUE)
  write.table(tab, "tabela_a2.csv", sep = ",", col.names = !file.exists("tabela_a2.csv"),
              row.names = F, append = T)
  
  pracma::toc()
}
 
# gama_0	0.5000			
# delta_0	1.0000			
# ARL_0	370.3978			
# mu_0	0.5000			
# sigma_0	0.5000			
# LIC	0.0791			
# LSC	1.4389	

```

```{r tabela a3, eval=FALSE}
# Para o número de corridas padrão cada iteração demora, em média, 5 min.
set.seed(2025)
for(x in c(.5, 1, 1.5, 2, 3, 5, 10, 20)){
  pracma::tic()
  
  tab = weibull_carta_xbar(gama0 = 3, delta0 = 6, vetordelta = x)
  #write.csv(tab, file="tabela1.csv", append = TRUE)
  write.table(tab, "tabela_a3.csv", sep = ",", col.names = !file.exists("tabela_a3.csv"),
              row.names = F, append = T)
  
  pracma::toc()
}

# gama_0	3.0000			
# delta_0	6.0000			
# ARL_0	370.3978			
# mu_0	2.7832			
# sigma_0	0.5393			
# LIC	2.0133			
# LSC	3.4486	

```


```{r tabela a4, eval=FALSE}
# Para o número de corridas padrão cada iteração demora, em média, 5 min.
set.seed(2025)
for(x in c(.5, 1, 1.5, 2, 3, 5, 10, 20)){
  pracma::tic()
  
  tab = weibull_carta_xbar(gama0 = 50, delta0 = 25, vetordelta = x)
  #write.csv(tab, file="tabela1.csv", append = TRUE)
  write.table(tab, "tabela_a4.csv", sep = ",", col.names = !file.exists("tabela_a4.csv"),
              row.names = F, append = T)
  
  pracma::toc()
}

# gama_0	50.0000			
# delta_0	25.0000			
# ARL_0	370.3978			
# mu_0	48.9219			
# sigma_0	2.4413			
# LIC	45.0241			
# LSC	51.6518

```


#### Graficamente

```{r}
dados = read.csv('tabela1_50mi.csv')
dados <- dados[, c(1:3)]
colnames(dados) <- c('Gama1', 'Delta1', 'ARL1')

x = dados[1:10, 1]

ggplot(dados[1:30, ], aes(x=Gama1, y=ARL1, group = Delta1, colour=factor(Delta1))) +
  geom_point() +
  geom_line() +
  labs(y = quote(CMS_1), x = 'Gama_1', colour = 'Delta_1') +
  scale_x_continuous(breaks = dados[1:10,1]) +
  theme_classic()
```


```{r}
plot(x=x, y=dados[1:10, 3], 'b', ylim=c(min(dados[, 3]), 45),
    pch = 16, cex = .5, ylab = "ARL_1")

lines(x = x, y = dados[11:20, 3], 'b', pch = 16, cex = .5, col='blue')
lines(x = x, y = dados[21:30, 3], 'b', pch = 16, cex = .5, col='red')
#lines(x = x, y = dados[31:40, 3], 'b')

par(mfrow=c(2,2))
plot(x = x, y = dados[41:50, 3], 'b', pch=16, cex = .5)
plot(x = x, y = dados[51:60, 3], 'b', pch=16, cex = .5)
plot(x = x, y = dados[61:70, 3], 'b', pch=16, cex = .5)
plot(x = x, y = dados[71:80, 3], 'b', pch=16, cex = .5)


```

